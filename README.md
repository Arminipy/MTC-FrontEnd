# Mobile Trade Center Web Client

## Development:

1. Install deps:

```bash
npm i
```

2. Start the dev server:

```bash
npm run dev
```

## Deployment

1. Install deps:

```bash
npm i
```

2. build the project:

```bash
npm run build
```

3. build the image and run the container:

```bash
docker compose up --build
```
